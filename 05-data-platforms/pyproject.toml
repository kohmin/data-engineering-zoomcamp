[project]
name = "05-data-platforms"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "kohmin", email = "61226634+kohmin@users.noreply.github.com" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
05-data-platforms = "05_data_platforms:main"

[build-system]
requires = ["uv_build>=0.12.4,<0.13.0"]
build-backend = "uv_build"
